---
topic: Session and Socket.IO Transport layer development
updated: 2026-07-17T01:52
---

- (decision) PRD scope is the development of the Session layer and Transport layer.
- (decision) Current-version Transport technology is Socket.IO; implementation mechanisms belong in addendum/architecture rather than PRD capability requirements.
- (decision) All existing architecture, planning, repository constraints, protocol docs, code, and tests are source inputs for this PRD.
- (decision) User chose a collaborative Coaching path to identify and resolve missing details rather than receive an inferred full draft.
- (event) Source extraction completed for architecture spine, brownfield docs/code/tests, and current official Socket.IO guarantees.
- (decision) Layering exists to reuse the Session and Transport contracts on both SDK/client and Server endpoints rather than making them SDK internals.
- (decision) Session, Transport implementations, and the application-level SDK are independent release boundaries.
- (decision) The SDK receives an injected Transport implementation instead of depending directly on one transport technology.
- (decision) Socket.IO is the current Transport implementation; WebSocket and Bluetooth are future pluggable implementations, not current delivery scope.
- (decision) Discovery will use Coaching path with a Vision-and-Features entry for this developer component.
- (assumption) Independent release may mean separately installable and semantically versioned packages; exact publication/install contract still requires user confirmation.
- (decision) Browser consumers directly install exactly the application SDK package and one selected Client Transport package.
- (decision) Browser consumers initialize the SDK Client API with a Transport and do not directly import or construct Session.
- (decision) The SDK consumes Session as an internal/transitive capability while remaining transport-implementation agnostic.
- (decision) Server consumers directly install and compose Session plus a selected Server Transport and do not depend on the application SDK.
- (assumption) Pending confirmation: callers choose/create Transport, but one Session exclusively owns its connect/disconnect/listener lifecycle; SDK delegates lifecycle through Session.
- (decision) Confirmed: SDK receives a Transport factory; each connection generation gets a configured but initially unconnected Transport instance.
- (decision) Confirmed: one Session exclusively owns the created Transport lifecycle, while SDK or Server owns the Session lifecycle.
- (decision) Transport.connect performs the implementation-specific asynchronous connection flow using configuration captured by that Transport.
- (decision) Future Bluetooth Transport may prompt the user to select an ESP32 device during connect; SDK and Session remain unaware of that interaction.
- (change) Latest desired Browser API passes one configured, unconnected Transport instance to new Client; this conflicts with the previously confirmed factory injection shape and requires explicit resolution.
- (decision) The same RemoteInputClient instance must support connect after an explicit disconnect; callers must not recreate Client for ordinary reconnection.
- (decision) Unexpected Transport disconnection must trigger automatic reconnection in the Session layer rather than requiring the application to call Client.connect again.
- (change) Reopened the factory-only reconnect decision: creating a new Transport object is not inherently required; distinguish a stable Transport object from fresh internal connection generations before choosing the injection API.
- (decision) Before an upper-layer request or notification is sent, Session checks Transport readiness and asks the same disconnected Transport instance to connect again; this is on-demand reconnect rather than mandatory background reconnect.
- (assumption) Pending design: after Transport reconnect, SDK must repeat session.open before the triggering business request because Session ready and SDK ready have different meanings.
- (decision) ProtocolSession does not own or duplicate a disconnected state; its public availability is only ready/not-ready and connection states belong exclusively to MessageTransport.
- (assumption) Pending confirmation: Session needs a private lifecycle gate so explicit disconnect suppresses request-triggered reconnect until a later explicit connect, without exposing disconnected as Session state.
- (event) PRD Update resumed using the latest Client, Session, and Transport discussion as the authoritative change signal.
- (override) Latest instance-injection model supersedes the earlier Transport factory decision: Browser creates a configured Transport and passes it to Client.
- (override) Latest narrow-port model supersedes Session-owned Transport lifecycle and Session-triggered reconnect: Session depends only on complete-message send and receive.
- (override) Transport disconnect no longer causes Session to immediately clear every pending Request; delivered Requests remain pending until their Response deadline.
- (decision) Session owns typed protocol correlation, unique requestId allocation, pending Request Map, runtime result validation, Notification dispatch, and per-Request Response timeout.
- (decision) Concurrent upper-layer Requests are submitted immediately to Transport; Session does not serialize them or own a second message queue.
- (decision) Transport exclusively owns connection, reconnect, complete-message queueing, fragmentation, cross-message windowing, cumulative ACK, retransmission, reassembly, and delivery-failure adjudication.
- (decision) A rejected Transport send is terminal: the message is abandoned and can never be delivered later by internal recovery.
- (decision) Client or Server composition root may observe Transport lifecycle state/events, but all protocol data remains Client to Session to Codec to Transport; Session never reads Transport state.
- (decision) Browser directly consumes SDK plus a chosen Client Transport; Server directly consumes Session plus a chosen Server Transport; current delivery implements Socket.IO only.
- (assumption) Pending confirmation: Client retains explicit connect and disconnect; explicit disconnect suppresses automatic recovery until a later connect, and the same Client/Transport can reconnect.
- (assumption) Pending confirmation: Session Response timeout starts after Transport confirms complete-message delivery; an early Response remains valid.
- (assumption) Pending confirmation: without a cross-connection resume protocol, all unfinished sends from an ended Socket generation fail and are never replayed on the new connection.
- (assumption) Pending confirmation: Client observes each new connected Transport generation, performs session.open once, and gates business APIs until open succeeds.
- (assumption) Pending Architecture decision: final package names, lifecycle state/on event shape, Session dispose API, and inputText/notification subscription naming.
- (decision) Retained from confirmed earlier flow: Client exposes parameterless connect/disconnect after Transport instance injection; explicit disconnect suppresses recovery until a later connect, and the same Client can connect again.
- (decision) Retained brownfield requirement: Session Response timeout starts after Transport confirms complete-message delivery; pending is registered before send and an early matching Response remains valid.
- (decision) Operation authoritative cache accepts only strictly increasing revisions; local optimistic projections do not enter the revision-bearing authoritative cache.
- (question) Phase blocker: may one pending Transport send survive and replay across a new Socket connection, or must every unfinished send from the ended connection fail with reconnect serving only later sends?
- (event) Finalize input reconciliation completed for latest conversation, brownfield code/constraints, and prior architecture/documents; gaps recorded in reconcile-update-*.md.
- (change) Reviewer correction: send rejection is local finality only; failures now distinguish not-delivered from delivery-unknown because a lost final ACK cannot prove the peer did not receive or process the message.
- (decision) Session receive registration stays attached to the stable Transport instance across internal reconnect generations until unsubscribe or Session disposal; delivered bytes have stable ownership.
- (decision) ClientManagedTransport actively connects and reconnects, while an accepted Server Transport starts with an accepted socket and is not required to implement active connect.
- (decision) Client uses a mandatory monotonic Transport connection-generation signal to run session.open once per generation and gates heartbeat and application readiness around that handshake.
- (decision) Session owns Ping/Pong correlation and timers but never changes Transport lifecycle; the Client or Server composition root handles heartbeat failure.
- (assumption) Until confirmed, an ended Socket generation rejects all its unfinished sends and never replays them on the new Socket; emitted data fails as delivery-unknown.
- (assumption) Until Architecture confirmation, reconnecting admits new sends to a bounded Transport queue with a default maximum of 3 attempts and 30 seconds total; explicitly stopped transports reject new sends as not-delivered.
- (event) Reviewer autofixes applied to PRD and technical addendum; one phase-blocking cross-generation delivery decision remains for user confirmation.
- (decision) client.connect resolves only after session.open succeeds for the current generation and Client is ready; default total deadline is 30 seconds and initial Transport connect is 10 seconds.
- (decision) A session.open ProtocolError, validation failure, or Client connect deadline failure tears down the current generation, leaves Client not-ready/error, rejects connect, and permits a later explicit connect.
- (decision) Heartbeat timeout starts only after Ping delivery; stopHeartbeat invalidates the entire run epoch, and Client forces a new generation by serial Transport disconnect/connect while Session never touches lifecycle.
- (decision) Client creates operationId before input.submit, permits explicit reuse, and exposes it on post-creation failures so delivery-unknown can be queried before retrying with the same ID.
- (decision) Duplicate inbound requests are rejected only while active or within a bounded 1024-entry/10-minute tombstone window; sender-side requestId nonreuse and Transport at-most-once remain the normal-path guarantees.
- (decision) Socket.IO frameSeq, messageId, and nextExpectedFrameSeq start at zero per generation, increment continuously, never wrap, and force generation failure before exhaustion.
- (decision) Confirmed by user: a closed Socket generation never transfers or replays unfinished sends onto a new Socket; locally untransmitted sends fail not-delivered, potentially emitted sends fail delivery-unknown, and reconnect serves only later messages.
- (event) Cross-generation replay phase blocker resolved and promoted from assumption to requirement; reconnect admission and post-fatal recovery policy is the remaining phase blocker.
- (decision) Confirmed by user: after an established Client Transport unexpectedly disconnects or becomes connection-fatal, it automatically reconnects for at most 3 attempts and 30 seconds; send rejects not-delivered during connecting/reconnecting, exhaustion enters error until explicit connect, and explicit disconnect suppresses recovery.
- (decision) Each connected generation performs exactly one session.open before Client ready, and application calls fail not-ready before that gate; this was included in the user-confirmed reconnect rule.
- (decision) The target SDK method name is inputText as explicitly specified by the user; notification and Client state subscription names remain an Architecture naming task.
- (decision) Session has an idempotent resource-release capability that never controls Transport lifecycle; Architecture chooses the final method name.
- (assumption) Response timeout remains 10 seconds by default with a provisional public configuration range of 1 to 120 seconds; Architecture owns review before Story decomposition and must preserve a finite positive bound.
- (event) Non-blocking package names, lifecycle event/type names, Session disposal name, and subscription names deferred to bmad-architecture and must be frozen before implementation Story decomposition.
- (override) User-confirmed reconnect admission supersedes the earlier assumption that reconnecting queues new sends: connecting/reconnecting now rejects every new send as not-delivered.
- (event) Final memlog audit completed: superseded factory/Session-lifecycle/reconnect decisions remain historical and are explicitly overridden or preserved as rejected alternatives; active decisions are represented in PRD/addendum, with naming/package items handed to Architecture.
- (change) Structural polish moved the protocol abnormal-behavior matrix beside FR-12 and moved the Architecture handoff list to the top of the addendum; other compression proposals were declined to preserve contract traceability.
- (change) Configured document standards completed: structural review preserved contract-heavy sections while moving the protocol error matrix and Architecture handoff; prose review removed ambiguous references and standardized Connection Generation and heartbeat run-cycle terminology without changing semantics.
- (event) PRD finalized
