---
scope: SDK application, ProtocolSession, MessageCodec and MessageTransport redesign
purpose: Build substrate for remote-copy SDK and protocol implementers
altitude: feature
updated: 2026-07-17T03:59
---

- (assumption) 快速路径；架构脊柱是本次唯一文件产物，主要读者为 SDK、Protocol 和 Server 实现者。
- (constraint) 用户要求 SDK 整体按 SDK 应用层、Session 层、Transport 层三个公开责任层重新设计，并要求 Transport 具备 send、receive、connect、disconnect 和 state。
- (constraint) 仓库强制依赖链为 RemoteInputClient -> ProtocolSession -> MessageCodec -> MessageTransport；Codec 不得删除，Transport 不得解析业务 JSON。
- (constraint) 当前只实现 Socket.IO Transport；不得实现或模拟 Bluetooth Transport。
- (constraint) packages/protocol 是协议唯一事实源；definitions 只导出类型、常量和契约，implementations 显式导出运行时实现。
- (decision) 采用分层端口-适配器架构：SDK 应用层依赖 Session 端口，Session 依赖 Codec 与 Transport 端口，具体 Codec/Transport 只在组合根装配；防止业务、协议编排和链路机制互相渗透。
- (decision) 三个公开责任层为 SDK Application、Protocol Session、Message Transport；MessageCodec 作为 Session 内部的独立编码与不可信输入校验端口保留，不作为 SDK 使用者操作的第四层。
- (decision) RemoteInputClient 拥有 sendInput、session.open 编排、capability 检查、operationId 生成、operation revision 缓存、SDK 状态订阅和稳定错误映射；不拥有 requestId、心跳 ID、协议编码或传输帧。
- (decision) ProtocolSession 保持业务无关；SDK 将 inputText 映射为 request("input.submit", { operationId, text })，Session 接收 typed method/body 并生成 requestId，不新增 inputText 专属 Session API。
- (decision) Session 的 pendingRequests 以 requestId 为键，值包含 method、resolve、reject 和 response timer；必须在 send 前登记，成功、失败、超时、发送失败、断开或 generation 替换时恰好清理一次。
- (decision) Response timeout 仅从 Transport send Promise 完成后开始；早于本地 Transport ACK 到达的 Response 仍可匹配；成功 body 必须依据 pending method 做运行时校验。
- (decision) 每次 Session connect 建立新 generation；重连/断开立即停止心跳、拒绝旧 pending request，并阻止旧 generation 的异步 handler 向新连接发送 Response。
- (assumption) MessageTransport 的公开 receive 采用 push 订阅：receive(listener: (event: TransportEvent) => void): Unsubscribe；事件联合承载完整 message、state change 和 error。内部原始帧入口命名为 ingestFrame/acceptFrame，不与公开 receive 混用。
- (decision) Transport send/receive 的边界单位始终是一条完整 Uint8Array 消息；公开 API 不提供 sendBatch，所谓批量传输由内部跨消息发送窗口实现且保留消息边界和顺序。
- (decision) Transport send 仅在该完整消息全部 DATA 帧获得累计 ACK 后完成；这不等于 Session Response，更不等于 operation 完成。
- (constraint) Socket.IO Transport 仅使用 protocol:frame；DATA 为 28-byte 大端 header 加 payload，ACK 固定 8 bytes；每方向 frameSeq、messageId、nextExpectedFrameSeq 从 0 独立连续递增且不得回绕。
- (constraint) Transport 默认 16 KiB chunk、8 帧窗口、2 秒 ACK timeout、最多 3 次重传、10 秒重组无进展 timeout；消息上限 256 KiB，队列上限 128 条且 4 MiB。
- (decision) Transport 的非法帧、重组超时、重传耗尽和断线是连接级 fatal：清空窗口、队列、重组缓存与计时器，拒绝全部未完成 send；单条超限或队列满只拒绝该 send。
- (decision) 连接组合根位于 SDK：每次 connect 创建一个 Transport 和一个 Session，先绑定 receive，再 Transport connect，再 session.open，成功后进入 ready 并启动心跳；默认显式重连。
- (decision) 状态所有权分离：Transport state 只表达链路；Session 表达协议连接与错误事件；SDK state 表达 idle/connecting/opening/ready/disconnecting/disconnected/error 及 peer、capability、operation 视图。
- (assumption) SDK 在 disconnect 后保留当前 connection generation 的 operation 快照供 UI 展示，在下一次 connect 开始时清空；旧 operation 不跨新连接刷新。
- (decision) requestId 只关联一次 Request/Response，operationId 关联长期输入操作，heartbeatId 关联 Ping/Pong，messageId/frameSeq 只属于 Transport；任何一层不得复用其他层 ID。
- (decision) Client 与 Server 使用相同 ProtocolSession；Client 发 request/收 notification，Server 注册 typed request handler/发 notification，每个 Socket 拥有独立 Transport 和 Session。
- (decision) SDK 依赖 ProtocolSessionContract 并通过 SessionFactory/TransportFactory 装配默认实现；Session/Transport 配置按层嵌套，避免 RemoteInputClient 平铺泄漏所有底层选项。
- (version) 现有仓库清单锁定 TypeScript 7.0.2、Turborepo 2.10.4、Socket.IO client/server ^4.8.1；本次不引入新技术。
- (direction) 实现迁移应先改 definitions 契约和测试 doubles，再改 ProtocolSession/Socket.IO 双端 Transport，再改 SDK 组合根与状态，最后改 Server、Client 和中文 README。
- (question) 公开 receive(event-listener) 的最终签名为快速路径假设；实现前若希望 message-only callback 或 AsyncIterable，需要先替换该假设以避免两个独立实现不兼容。
- (decision) TransportEvent 维持判别联合：message 事件只携带完整 Uint8Array，state 事件携带 TransportState，error 事件携带结构化 TransportError；receive(listener) 返回幂等 Unsubscribe，监听器异常不得打断 ACK、状态推进或其他监听器。
- (decision) TransportState 采用 idle/connecting/connected/disconnecting/disconnected/error；并发 connect 共享同一 in-flight Promise，已连接 connect 为 no-op，并发 disconnect 共享同一 in-flight Promise且幂等。fatal 后保持 error，底层 socket close 不得覆写为 disconnected；显式 disconnect 后进入 disconnected。
- (decision) SDK connect Promise 只在 Transport 已连接、session.open 成功并完成 capability/peer 写入后 resolve；SDK 对外不把短暂 transport-connected 当作 ready。
- (decision) SDK sendInput 校验非空与 64 KiB UTF-8 上限，生成 operationId，并调用 input.submit；Response 只表示下游 accepted，最终状态来自 operation.status 或 operation.get。
- (decision) OperationStore 是 SDK 内部唯一 operation 快照所有者；只接受更高 revision，允许真实通知替换同 revision 的本地 synthetic accepted；订阅与 currentOperation 视图从该 store 派生。
- (decision) 错误按 TransportError、SessionError/ProtocolResponseError、SDK stable error code 三层传播；底层错误保留 cause，SDK 不向应用暴露 Socket.IO 特有异常作为公共契约。
- (decision) SDK 内部拆为 RemoteInputClient facade、SdkStateStore、OperationStore 和默认 composition root；这些内部 store 不从 packages/sdk/src/index.ts 导出。
- (decision) 单个 MessageTransport 实例只能由一个 ProtocolSession 拥有；Client/Server 每个连接各建一对实例，禁止两个 Session 同时消费同一 receive 流。
- (decision) 公开协议消息保持 request/response/notification/ping/pong 五种判别联合；Session 控制心跳，Notification 无 requestId，Transport ACK 不进入 ProtocolMessage。
- (decision) 自动验证按 protocol、sdk、server 三层分别覆盖，再执行 pnpm check 与 pnpm build；真实联调只允许 session.open，不发送非空 input.submit。
- (assumption) RemoteInputClient 继续限制同一时刻仅一个 active input operation；ProtocolSession 仍支持最多 128 个并发 pending request。若产品需要并行输入，应在独立需求中改 SDK policy，不改 Session。
- (decision) SDK operation cache 仅为进程内内存视图，不在本次架构引入持久化；输入历史仍由 apps/client 负责。
- (decision) Session 对未知、重复或迟到 Response/Pong 和 Codec 校验失败发布 Session error，但不默认断链；Transport 非法帧与可靠性交付失败属于 fatal 并断链。
- (decision) 本次只重构进程内 SDK/Protocol/Transport 边界，不新增服务、存储或部署环境；运行拓扑仍为 Browser Client 连接单一 Node Server 的 Socket.IO endpoint，Server 同时托管静态资源。
- (decision) 修正 OperationStore 规则：权威缓存只接受 incoming.revision > cached.revision；删除真实状态替换同 revision synthetic 的例外。本地 optimistic 状态不得进入带 revision 的权威缓存。
- (decision) 公共 operation state 仅为 accepted/processing/succeeded/failed；stage 只描述当前下游专属阶段；succeeded 只表示当前协议下游完成自身职责。
- (decision) AD-6 的 receive push event-union 签名正式采用，不再标为假设或 Deferred；subscribe -> receive 是本次明确的公共契约迁移，必须同步全部实现、测试替身、调用方、exports 与 README，不保留双接口回退。
- (decision) TransportState 保持 idle/connecting/connected/disconnected/error 五态；不新增公开 disconnecting。单实例 connecting 时 connect 复用 in-flight Promise，connected 时幂等；显式 disconnect 幂等。
- (decision) SDK ConnectionState 保持 idle/connecting/connected/ready/disconnected/error；Transport 建链后、session.open 完成前为 connected，connect Promise 只在 ready 后完成。RemoteInputState 现有字段全部保持。
- (decision) RemoteInputClientOptions 保留现有 clientName/createRequestId/createOperationId/createTransport/requestTimeoutMs/heartbeatIntervalMs/heartbeatTimeoutMs；createSession 仅作为新增可选注入点并接收 normalized session options，返回未连接 Session。
- (decision) SDK 每次 connect 创建新 generation，后一次调用使旧 generation 失效并以 connection-superseded 拒绝旧调用；旧 transport 创建完成后必须 disconnect。disconnect 使当前 generation 失效、幂等等待清理，被取消 connect 不得进入 ready。
- (decision) Codec decode 对成功 Response 只校验版本、envelope 与 requestId，body 保持 unknown；Session 匹配 pending request 后必须按 pending method 运行时校验 result。Codec 仍直接校验 Request/Notification body 与 ProtocolError。
- (decision) Transport sequence exhaustion 是 connection-fatal：拒绝触发 send，使用 sequence-exhausted 错误，清理全部连接资源并进入 error。
- (decision) Transport fatal 原子清理后只发布一次对应 error 和 state=error，底层 socket close 不得覆写；Session 不依赖事件先后，任一 fatal/disconnected 信号只执行一次 generation 终止清理。显式 disconnect 可使 error 进入 disconnected。
- (decision) SDK 在 session.open 和 input.submit 发送前已安装当前 generation 的 Session 订阅；connected 阶段早到的 session.peers 按当前 generation 缓存，input.submit Response 前早到的 operation.status 立即按 revision 合并。
- (decision) 同一连接内 session.open 幂等并保留 peer identity；clientName 变化时更新名称且只触发一次 peers 广播。Server operation 去重域固定为 (connectionId, operationId)。
- (decision) Server 接受的 input job 在连接断开后继续到 terminal，避免已开始副作用被重试；断开后 notification sink 变为 no-op并释放 Session 引用。新连接是新 operation 去重域，不能查询旧连接 operation。
- (decision) 任何公共契约变化同时更新 protocol definitions、实现、SDK index、exports tests 和中文 README；跨 workspace 实现执行全部三组测试、check 与 build。
- (decision) SDK OperationStore 最多保留 1000 条权威快照；超限时优先淘汰最旧 terminal operation，当前 active operation 不得淘汰。Client 历史可保存不可反写 SDK 的独立投影。
- (event) load-bearing inputs reconciled against user constraints, AGENTS.md, SDK/Client, Protocol/Transport and Server; clear conflicts applied
- (decision) 修正 operation 去重域：operationId 在单个 Server 进程生命周期内是全局幂等键，不再按 connectionId 分区。相同 operationId+相同 text 复用既有 operation；相同 ID+不同 text 返回 operation.id-conflict。
- (decision) Server operation 记录独立于连接，accepted job 断线后继续；Session 只作为可解绑 subscriber。新连接以同 operationId 重试或查询时重新订阅并收到当前/后续状态，避免副作用重复。
- (decision) RemoteInputClient.sendInput 增加可选 operationId 输入；缺省仍生成。SendInputError 保留本次 operationId，使 Response 丢失后的调用者可复用同一 ID 重试。
- (decision) requestId 在一个 Session generation 内永久不得复用，不只是 pending 期间唯一；Session 必须拒绝 ID factory 返回已签发 ID，generation 更换后方可清理 issued IDs。
- (decision) 同一 pending request 的第一个 terminal signal 胜出：若 Response 已匹配并完成，随后 transport.send rejection 必须忽略；若 send failure 先删除 entry，随后 Response 作为 unknown/late 报告。
- (decision) Transport receive 注册同步生效且不 replay 历史事件；listener 在内部 state mutation 后同步调用。connect/disconnect 的最终 state 事件先于对应 Promise resolve。fatal 顺序固定为原子清理并拒绝 sends、写 state=error、发布 state error 事件、再发布 error 事件。
- (decision) 非 fatal 的远端/底层 socket close 统一进入 disconnected；只有连接建立失败、非法 frame、重组/ACK/序号 fatal 进入 error。fatal 引发的后续 socket close 必须抑制 disconnected event。
- (decision) Socket.IO chunkPayloadBytes 是双端配对配置且当前不协商；空消息编码为 1 个 totalMessageBytes=0/payloadBytes=0 的 DATA frame；maxRetransmissions=3 表示初次发送之外最多三轮 Go-Back-N 重传。
- (decision) OperationStore 接受当前 generation 的未知 operation.status 并创建记录；状态迁移只能 accepted->accepted/processing/succeeded/failed 或 processing->processing/succeeded/failed，succeeded/failed terminal 后不可再变。
- (decision) OperationStore currentOperation 始终是最后一个成功应用且仍在缓存的 status；overflow 优先淘汰最旧 terminal，若没有可淘汰项则拒绝新增未知 operation 并发布 operation-cache-full。
- (decision) SDK error 映射固定：本地 busy 才是 input-busy；Transport queue/message resource rejection 映射 SendInputError request-failed且连接保持 ready；ProtocolResponseError 映射 peer-error；Codec 映射 invalid-message；建链失败映射 transport-connect-failed；建链后 fatal 映射 transport-error。
- (direction) 架构正文增加 Brownfield Migration Ledger；[ADOPTED] 表示用户/现有硬约束，未继承的新决策显式标 [NEW]，避免把目标行为误写成当前现实。
- (decision) Session 每个 generation 最多签发 1,000,000 个 requestId；达到上限后以 request-id-capacity 拒绝新 request 并要求重连，不能淘汰 issued ID 后复用。
- (decision) Server 将 operation 状态缓存与幂等 tombstone 分离：最多保留 1000 个状态快照，但进程生命周期内保留最多 100,000 个 (operationId,textHash) tombstone；状态已淘汰的重复 ID 返回 operation.expired，绝不重新执行。
- (decision) Server tombstone 达到 100,000 后以 operation.capacity-exhausted 拒绝新的 operationId，不淘汰旧 tombstone；Server restart 开启新的幂等域。
- (decision) Session 同一 generation 内 outbound requestId、outbound heartbeatId 和 inbound requestId 均不得复用；重复 inbound request 不再执行 handler，返回 request.duplicate 或发布协议错误。各 seen/issued 集合上限 1,000,000，耗尽要求重连。
- (decision) Transport 对单连接内完整消息保证有序、保留边界且最多向上交付一次；队列 128/4MiB 统计所有等待发送与等待 ACK 的消息，不能只统计尚未入窗口部分。
- (decision) Server 1000 条 status cache 优先淘汰最旧 terminal，active 不淘汰；tombstone 保留。Operation subscriber 在 disconnect 时解绑，在 terminal 通知完成后释放。
- (decision) Transport lifecycle event 以原子 batch 分发；listener 内重入 connect/disconnect 排队到当前 batch 完成且原 lifecycle Promise settle 后 FIFO 执行。fatal 的 state=error 与 error 是同一 batch，避免重入改变事件序列。
- (decision) Transport 分片 canonical：chunkCount=max(1,ceil(total/chunkPayloadBytes))；除末片外 payloadBytes 必须等于 chunkPayloadBytes，末片等于剩余字节，空消息末片为0；接收端拒绝非 canonical frame。
- (decision) ACK progress 仅指累计 ACK 严格推进 sendBase；重复/回退 ACK 不重置重传 timeout。重组 progress 仅指接受恰好 nextExpected frame/chunk 并推进序号；重复/越序 DATA 不重置 10s timeout。
- (decision) Server input.submit 在单个无 await 临界区内原子完成 operationId claim、textHash 冲突检查、subscriber 绑定和首个 accepted revision=1 snapshot，然后才启动 job/发送通知或 Response。
- (decision) Server duplicate submit/operation.get 在同一临界区绑定 subscriber并读取 snapshot；状态更新原子递增 revision和替换 snapshot后获取 subscriber快照，再异步通知，防止订阅与读取之间漏状态。
- (decision) Transport/Session listener fanout 使用事件开始时的注册顺序快照；dispatch 期间 subscribe/unsubscribe 只影响下一事件，取消函数幂等。
- (event) spine finalized after lint, input reconciliation, reviewer gate, and three PASS spot-checks
- (event) Architecture update resumed from the existing memlog; user selected Coaching path and confirmed the Architecture Spine is the only deliverable.
- (constraint) The finalized 2026-07-17 PRD and addendum are binding update inputs; they supersede prior factory injection, Session-owned lifecycle, event-union receive, per-connect Transport/Session reconstruction, explicit-only reconnect, and disconnect-driven bulk pending cleanup decisions.
- (constraint) Brownfield reality remains a private pnpm/Turborepo TypeScript monorepo; current @remote-copy/protocol combines definitions, validation, Codec, Session, and both Socket.IO transports, causing Session/server consumers to inherit socket.io-client.
- (version) Reality-checked from manifests and lockfile: TypeScript 7.0.2, Turborepo 2.10.4, Socket.IO client/server 4.8.3; no new framework or runtime technology is introduced.
- (constraint) Official Socket.IO 4.x documentation, last updated 2026-06-08, still guarantees ordering only for events that arrive, defaults to at-most-once arrival, buffers disconnected Client emits, and does not guarantee connection state recovery; the product DATA/ACK layer must therefore own delivery and suppress old-generation offline-buffer resurrection.
- (direction) Preserve the layered ports-and-adapters paradigm and existing AD IDs, amend superseded AD rules in place, and add new AD IDs only for invariants that have no existing ownership.
- (question) Freeze independently publishable package topology, including Codec/validation ownership, whether Socket.IO Client and accepted Server transports share one package, and dependency/peer version rules.
- (question) Freeze the race-free Managed Transport lifecycle snapshot/subscription contract, lifecycle states and diagnostics, Session dispose semantics, and Client state/notification subscription names before Story decomposition.
- (decision) Confirmed by user: publish four public packages: @remote-copy/protocol owns protocol definitions, layered contracts, runtime validation and JsonMessageCodec but no Session or concrete Transport; @remote-copy/session owns ProtocolSession; @remote-copy/transport-socket-io owns both roles via ./client and ./server with one shared wire implementation; @remote-copy/sdk owns RemoteInputClient and has no concrete Transport dependency. This binds package ownership and prevents Session/Transport release coupling and Socket.IO wire version drift.
- (decision) Browser directly installs @remote-copy/sdk plus @remote-copy/transport-socket-io and imports the client subpath; Server directly installs @remote-copy/session plus the same Transport package and imports the server subpath; protocol/session dependencies remain transitive.
- (decision) Workspace package dependencies publish with same-major caret compatibility through workspace:^; runtime libraries needed by a public package are normal dependencies rather than peer-only prerequisites, and cross-package errors use stable discriminants/codes with cause instead of instanceof as the compatibility contract.
- (decision) Confirmed by user: RemoteInputClient owns one caller-injected reusable Client Transport for its lifetime; each explicit Client connection cycle creates one fresh ProtocolSession, while all automatic Transport reconnect generations inside that cycle reuse the same Session and stable receive registration.
- (decision) Confirmed by user: explicit client.disconnect ends the current application connection cycle by stopping heartbeat, synchronously and idempotently disposing the Session without Session touching Transport lifecycle, then disconnecting the Transport; a later connect creates a fresh Session over the same Transport.
- (decision) Concurrent client.connect calls are single-flight: connecting callers share one Promise, ready calls resolve idempotently, and disconnect cancels an in-flight connect and prevents all late open/lifecycle results from mutating the ended cycle.
- (decision) ProtocolSession.dispose(): void is the final Session resource-release API; it is synchronous, idempotent, permanently rejects further use, unsubscribes receive, invalidates heartbeat/handler callbacks, clears timers, and rejects remaining pending Requests with session-disposed without calling Transport lifecycle methods.
- (decision) Confirmed by user: @remote-copy/protocol defines MessageTransport as the Session-only data port with exactly send(Uint8Array) and receive(messageListener); receive delivers complete owned byte snapshots, has no lifecycle/error union, allows exactly one active receiver, registers synchronously without replay, and returns an idempotent unsubscribe.
- (decision) Confirmed by user: ClientTransport extends MessageTransport with kind, state, getLifecycleSnapshot, subscribeLifecycle, connect and disconnect; ServerTransport extends MessageTransport with the same observation/close surface but no active connect requirement. Concrete SocketIoClientTransport and SocketIoServerTransport implement these roles.
- (decision) Confirmed by user: TransportState is idle|connecting|connected|reconnecting|disconnected|error. subscribeLifecycle synchronously emits the current immutable snapshot before returning and then emits post-mutation snapshots; listener failure is isolated. Connected snapshots carry a positive monotonic generation unique for the Transport instance.
- (decision) Confirmed by user: connecting snapshots expose the connect deadline; reconnecting snapshots expose attempt, maxAttempts, total recovery deadline and latest structured error. Unexpected active-client termination transitions directly to reconnecting, while accepted-server termination becomes disconnected; connection-fatal enters reconnecting when recovery is allowed and only initial failure or recovery exhaustion becomes terminal error.
- (decision) Confirmed by user: ClientTransport.connect is idempotent when connected and joins the active single-flight while connecting or reconnecting; send accepts new work only in connected and otherwise rejects immediately as not-delivered.
- (decision) Confirmed by user: RemoteInputClient constructor takes one caller-created ClientTransport plus options; public commands are parameterless connect/disconnect, inputText(text,{operationId?}), local/remote operation access, and explicit subscriptions. sendInput and Transport factory aliases are removed atomically.
- (decision) Confirmed by user: ClientConnectionState is idle|connecting|opening|ready|reconnecting|disconnecting|disconnected|error. opening means the Transport generation is connected but session.open is pending; connected is not a Client application state. Automatic recovery follows ready->reconnecting->opening->ready.
- (decision) Confirmed by user: Client public subscription names are subscribeState, subscribeNotification, and subscribeOperation. subscribeState synchronously emits the current immutable snapshot; subscribeNotification has future-only validated event semantics; subscribeOperation synchronously emits a cached snapshot when present and thereafter only successfully applied strictly newer revisions.
- (decision) Confirmed by user: getState, getOperationStatus, and refreshOperationStatus retain snapshot/query separation; inputText is rejected unless Client is ready, and application state is never inferred from Transport state alone.
- (decision) Confirmed by user: every MessageTransport.send rejection is a structural TransportSendError with kind=transport-send, stable code, scope=call|connection-generation, delivery=not-delivered|delivery-unknown and optional cause; delivery is independent of failure reason/scope and cannot be inferred from lifecycle state.
- (decision) Confirmed by user: Transport lifecycle diagnostics use a distinct TransportLifecycleError rather than pretending to be a message send result; cross-package consumers use stable discriminants and isTransportSendError-style guards, never instanceof as the compatibility contract.
- (decision) Confirmed by user: SDK renames SendInputError to InputTextError and distinguishes input-empty, input-too-large, not-ready, unsupported, busy, not-delivered, delivery-unknown, response-timeout, remote-error, invalid-response and session-disposed. It carries operationId:string|null plus cause, with null permitted only before ID creation.
- (decision) Confirmed by user: delivery-unknown is never advertised as safely auto-retryable; recovery queries or retries the same operationId so application idempotency, not Transport replay, controls duplicate side effects.
- (decision) Confirmed by user: ProtocolSessionContract exposes request, notify, handleRequest, subscribeNotification, subscribeError, startHeartbeat, stopHeartbeat and dispose only; it exposes no connect, disconnect, state, transport property or transport-state event.
- (decision) Confirmed by user: ProtocolSession synchronously acquires the sole MessageTransport receiver at construction and releases it on dispose. Notification and Session diagnostic errors have separate future-only subscriptions with listener isolation; request-local send failure, response timeout and remote ProtocolError settle only the matching request and are not redundantly broadcast.
- (decision) Confirmed by user: Session option names are responseTimeoutMs and pongTimeoutMs, replacing requestTimeoutMs and heartbeatTimeoutMs. Response timeout defaults to 10000 ms and accepts integer values from 1000 through 120000 ms; maxPendingRequests defaults to 128, heartbeat interval to 15000 ms and Pong timeout to 10000 ms.
- (decision) Confirmed by user: Session constructor dependencies include MessageTransport, optional MessageCodec and request/heartbeat ID factories. Pending registration precedes send, concurrent requests immediately call send, response timing begins after delivery if still pending, and the first terminal signal wins.
- (decision) Confirmed by user: Socket.IO active-client recovery uses at most three attempts inside a 30-second total budget with deterministic pre-attempt delays [0 ms, 1000 ms, 3000 ms]; each attempt has at most 10 seconds and is truncated to the remaining total budget. Initial explicit connect is one 10-second attempt and does not auto-retry before a connection has ever been established.
- (decision) Confirmed by user: Transport disconnect synchronously records explicit stop intent, cancels current connect/recovery/socket listeners/timers, rejects affected waiters, and publishes disconnected before its Promise resolves. A connect invoked during cleanup is FIFO-queued after it; stale callbacks are rejected by lifecycle epoch.
- (decision) Confirmed by user: lifecycle-listener reentrant connect/disconnect commands use the same FIFO transition queue and cannot interleave the current snapshot fanout. Socket.IO offline buffering and connection-state recovery are not product recovery mechanisms; each generation uses isolated Socket listeners/state and old frames never revive.
- (event) Coaching elicitation complete: package topology, explicit Session cycle, data/lifecycle ports, Client API/state, error taxonomy, Session API/timeouts, and reconnect policy are all user-confirmed; no phase-blocking architecture question remains.
- (question) Preflight blocker: freeze structural Session request-rejection and diagnostic-error discriminants/codes so SDK maps response timeout, remote error, invalid result, disposal and heartbeat timeout without instanceof/message control flow.
- (question) Preflight blocker: freeze Client cycle terminal cleanup and subsequent connect behavior after initial/open/recovery failure, including connect calls while opening, reconnecting, disconnecting or error.
- (question) Preflight blocker: freeze exact TransportLifecycleSnapshot field names/units/attempt semantics and lifecycle error codes, plus state/snapshot consistency.
- (question) Preflight blocker: freeze exact four-package export maps and same-major protocol/wire compatibility discipline; direct Browser/Server consumers must not need a separately declared protocol package.
- (decision) Confirmed by user: SessionRequestError is structural kind=session-request with code transport-send-failed|response-timeout|remote-error|invalid-response|pending-capacity|session-disposed, plus method, requestId:string|null and cause; it rejects only the matching call and is not broadcast.
- (decision) Confirmed by user: SessionDiagnosticError is structural kind=session-diagnostic with code invalid-message|unknown-response|unknown-pong|handler-failed|heartbeat-send-failed|heartbeat-timeout plus cause; subscribeError receives only diagnostics and cross-package mapping never depends on message text or instanceof.
- (decision) Confirmed by user: heartbeat generation-scope send failure stops the current heartbeat run and relies on Transport lifecycle; call-scope capacity failure starts no Pong timer and retries on the next interval; only a current cycle/current generation heartbeat-timeout forces Client Transport disconnect then connect.
- (decision) Confirmed by user: Client connect joins the readiness Promise while connecting/opening/reconnecting, resolves idempotently while ready, queues behind disconnecting, and from error disposes the old Session then creates a fresh Session/cycle. All async results require matching clientCycleId plus Transport generation.
- (decision) Confirmed by user: initial session.open failure disposes immediately because business APIs were never enabled. After a previously-ready cycle reaches recovery exhaustion or a new-generation open failure, Client enters error and stops heartbeat but does not dispose solely because of lifecycle; delivered Requests retain their Response deadlines. A later explicit disconnect/connect disposes the old Session and remaining calls become session-disposed.
- (decision) Confirmed by user: TransportLifecycleSnapshot is an exact discriminated union: idle/disconnected have generation=null; connecting has generation=null and deadlineAt; connected has positive generation; reconnecting has generation=null, previousGeneration, 1-based current attempt, maxAttempts, deadlineAt and lastError; error has generation=null and error. deadlineAt is Unix epoch milliseconds for observation while budgets use a monotonic clock.
- (decision) Confirmed by user: TransportLifecycleError is structural kind=transport-lifecycle with code connect-timeout|connect-failed|connection-lost|invalid-frame|reassembly-timeout|retransmission-exhausted|sequence-exhausted|connect-cancelled|internal plus cause. transport.state is derived from the current snapshot; accepted Server Transport starts connected at generation 1.
- (decision) Confirmed by user: exact exports are protocol root and /definitions for types/constants/ports without runtime, protocol /implementations for validation/JsonMessageCodec/structural guards, session root for ProtocolSession and Session errors/guards with permitted protocol re-exports, transport-socket-io /client and /server only with private shared core and no root runtime aggregator, and sdk root for RemoteInputClient/InputTextError/application types.
- (decision) Confirmed by user: same-major minor/patch releases must be bidirectionally compatible for existing protocol envelopes/method schemas and DATA/ACK wire; breaking schema or wire changes require a package major plus protocol version or frameVersion change, unknown versions fail deterministically, and CI keeps golden compatibility fixtures against the major's lowest supported format.
- (decision) Confirmed by user: Socket.IO client/server libraries are normal Transport package dependencies; client/server subpaths cannot cross-import, and Browser bundle verification must prove Server runtime exclusion.
- (constraint) Current deployment envelope is trusted local/LAN use and is not a public-network security boundary. TLS, authentication, Origin allowlisting and rate limiting require architecture review before any public exposure.
- (event) Architecture preflight blockers resolved by user confirmation; distillation may proceed without assumptions or open phase blockers.
- (event) Spine redistilled from active memlog decisions with AD-1 through AD-18 IDs preserved; superseded factory, Session lifecycle, event-union receive and disconnect-driven pending cleanup seed removed.
- (change) Input reconciliation fixes distinguished the 30-second Client readiness budget from the 10-second Transport attempt, required open-failure generation closure, completed heartbeat and inbound Request rules, closed structural Transport/Session/SDK error unions, and bound Managed Transport FIFO command semantics.
- (change) Brownfield reconciliation clarified install package versus import subpath, declared all internal edges as normal dependencies with no internal peers, made root test:protocol aggregate protocol/session/transport plus compatibility and bundle checks, and moved non-empty input.submit coverage off real Socket.IO integration.
- (change) Reviewer Gate fixes closed Client readiness/open linearization, nested Transport delivery mapping, exact Client state/connect errors, fresh Socket.IO Socket/Manager per generation, Operation transition rules, mandatory protocol re-exports, reproducible Node toolchain pin, bounded inbound handler admission and diagnostic redaction.
- (event) Reviewer Gate complete: deterministic lint 0 findings; rubric, reality/current, adversarial divergence and security/operations rechecks all PASS with no unresolved Critical, High or Medium findings.
- (event) spine finalized
