{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["VITE_BASE_PATH", "VITE_PROTOCOL_DEBUG"]
    },
    "build:pages": {
      "dependsOn": ["^build"],
      "outputs": ["dist-pages/**"],
      "env": ["VITE_BASE_PATH", "VITE_PROTOCOL_DEBUG"]
    },
    "check": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true,
      "env": [
        "HOST",
        "PORT",
        "INPUT_MODE",
        "PROTOCOL_DEBUG",
        "REMOTE_INPUT_VID",
        "REMOTE_INPUT_PID",
        "VITE_PROTOCOL_DEBUG"
      ]
    },
    "start": {
      "dependsOn": ["build"],
      "cache": false,
      "persistent": true,
      "env": [
        "HOST",
        "PORT",
        "INPUT_MODE",
        "PROTOCOL_DEBUG",
        "REMOTE_INPUT_VID",
        "REMOTE_INPUT_PID",
        "VITE_PROTOCOL_DEBUG"
      ]
    },
    "install:user": {
      "dependsOn": ["build"],
      "cache": false
    },
    "uninstall:user": {
      "cache": false
    }
  }
}
